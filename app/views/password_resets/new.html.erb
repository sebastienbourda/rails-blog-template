<div class="flex min-h-full items-center justify-center mt-16 px-4 py-12 sm:px-6 lg:px-8">
  <div class="w-full max-w-sm space-y-10">
    <div>
      <h2 class="mt-10 text-center text-2xl font-bold leading-9 tracking-tight text-gray-900">Ask for a password reset</h2>
      <p class="text-center text-lg tracking-tight text-gray-900">Enter your email address below to reset your password.</p>
      <% if @password_reset.errors.any? %>
        <div id="error_explanation" class="bg-red-100 my-4 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
          <strong class="font-bold">Oops!</strong>
          <span class="block sm:inline"><%= pluralize(@password_reset.errors.count, "error") %> prohibited this password_reset from being saved:</span>
          <ul class="list-disc list-inside">
            <% @password_reset.errors.full_messages.each do |password_reset| %>
              <li><%= password_reset %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
    </div>

    <%= render "form" %>
  </div>
</div>
